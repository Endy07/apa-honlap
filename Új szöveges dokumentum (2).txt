<!DOCTYPE html>
<html lang="hu" data-theme="hajnal">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Kárai Mihály – Festőművész</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400;1,500&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">
<style>
/* ═══════════════════════════════════════════════════════════
   60-30-10 COLOR RULE — 7 THEMES
   60% = háttér (domináns) · 30% = másodlagos (szöveg/kártyák) · 10% = accent (kiemelések)
   ═══════════════════════════════════════════════════════════ */

/* ── Téma 1: Éjszaka (Sötét) ── */
[data-theme="ejszaka"] {
    --bg-primary: #080808;
    --bg-secondary: #141414;
    --bg-tertiary: #1e1e1e;
    --bg-card: #1a1a1a;
    --text-primary: #f0e8d8;
    --text-secondary: #b8a888;
    --text-muted: #7a6e5a;
    --accent: #d4a853;
    --accent-hover: #e8c068;
    --accent-subtle: rgba(212, 168, 83, 0.08);
    --accent-glow: rgba(212, 168, 83, 0.25);
    --border: rgba(240, 232, 216, 0.08);
    --border-hover: rgba(212, 168, 83, 0.3);
    --shadow: rgba(0, 0, 0, 0.6);
    --overlay: rgba(8, 8, 8, 0.85);
    --grain-opacity: 0.035;
}

/* ── Téma 2: Hajnal (Világos) ── */
[data-theme="hajnal"] {
    --bg-primary: #f7f2ea;
    --bg-secondary: #ede5d8;
    --bg-tertiary: #e3d9c8;
    --bg-card: #fffdf8;
    --text-primary: #2a2218;
    --text-secondary: #6b5a48;
    --text-muted: #9a8a78;
    --accent: #c4884a;
    --accent-hover: #d49858;
    --accent-subtle: rgba(196, 136, 74, 0.08);
    --accent-glow: rgba(196, 136, 74, 0.2);
    --border: rgba(42, 34, 24, 0.08);
    --border-hover: rgba(196, 136, 74, 0.3);
    --shadow: rgba(42, 34, 24, 0.1);
    --overlay: rgba(247, 242, 234, 0.9);
    --grain-opacity: 0.025;
}

/* ── Téma 3: Oázis (Sötét) ── */
[data-theme="oazis"] {
    --bg-primary: #0e1a0e;
    --bg-secondary: #162a16;
    --bg-tertiary: #1e3a1e;
    --bg-card: #1a2e1a;
    --text-primary: #e8f0e0;
    --text-secondary: #a8c098;
    --text-muted: #6a8a5a;
    --accent: #d4a853;
    --accent-hover: #e8c068;
    --accent-subtle: rgba(212, 168, 83, 0.08);
    --accent-glow: rgba(212, 168, 83, 0.2);
    --border: rgba(232, 240, 224, 0.08);
    --border-hover: rgba(212, 168, 83, 0.3);
    --shadow: rgba(0, 0, 0, 0.5);
    --overlay: rgba(14, 26, 14, 0.9);
    --grain-opacity: 0.03;
}

/* ── Téma 4: Vászon (Világos) ── */
[data-theme="vaszon"] {
    --bg-primary: #f5f2ec;
    --bg-secondary: #ebe6dc;
    --bg-tertiary: #e0dac8;
    --bg-card: #faf8f2;
    --text-primary: #2a2a28;
    --text-secondary: #5a5a52;
    --text-muted: #8a8a80;
    --accent: #5a7a4a;
    --accent-hover: #6a8a5a;
    --accent-subtle: rgba(90, 122, 74, 0.08);
    --accent-glow: rgba(90, 122, 74, 0.2);
    --border: rgba(42, 42, 40, 0.08);
    --border-hover: rgba(90, 122, 74, 0.3);
    --shadow: rgba(42, 42, 40, 0.08);
    --overlay: rgba(245, 242, 236, 0.9);
    --grain-opacity: 0.02;
}

/* ── Téma 5: Tűz és Jég (Sötét) ── */
[data-theme="tuz-es-jeg"] {
    --bg-primary: #0a0c14;
    --bg-secondary: #12162a;
    --bg-tertiary: #1a1e3a;
    --bg-card: #141830;
    --text-primary: #e8e0f0;
    --text-secondary: #a898c0;
    --text-muted: #6a5a8a;
    --accent: #e04060;
    --accent-hover: #f05070;
    --accent-subtle: rgba(224, 64, 96, 0.08);
    --accent-glow: rgba(224, 64, 96, 0.25);
    --border: rgba(232, 224, 240, 0.08);
    --border-hover: rgba(224, 64, 96, 0.3);
    --shadow: rgba(0, 0, 0, 0.6);
    --overlay: rgba(10, 12, 20, 0.92);
    --grain-opacity: 0.03;
}

/* ── Téma 6: Pasztell (Világos) ── */
[data-theme="pasztell"] {
    --bg-primary: #faf5f2;
    --bg-secondary: #f0e8e0;
    --bg-tertiary: #e8ddd4;
    --bg-card: #fffaf8;
    --text-primary: #3a2a28;
    --text-secondary: #7a5a52;
    --text-muted: #a89088;
    --accent: #c47a6a;
    --accent-hover: #d48a7a;
    --accent-subtle: rgba(196, 122, 106, 0.08);
    --accent-glow: rgba(196, 122, 106, 0.2);
    --border: rgba(58, 42, 40, 0.08);
    --border-hover: rgba(196, 122, 106, 0.3);
    --shadow: rgba(58, 42, 40, 0.08);
    --overlay: rgba(250, 245, 242, 0.9);
    --grain-opacity: 0.02;
}

/* ── Téma 7: Erdő (Sötét) ── */
[data-theme="erdo"] {
    --bg-primary: #0e0c08;
    --bg-secondary: #1a1610;
    --bg-tertiary: #262018;
    --bg-card: #1e1a12;
    --text-primary: #e8dcc8;
    --text-secondary: #b0a088;
    --text-muted: #7a6e58;
    --accent: #c49040;
    --accent-hover: #d4a050;
    --accent-subtle: rgba(196, 144, 64, 0.08);
    --accent-glow: rgba(196, 144, 64, 0.2);
    --border: rgba(232, 220, 200, 0.08);
    --border-hover: rgba(196, 144, 64, 0.3);
    --shadow: rgba(0, 0, 0, 0.6);
    --overlay: rgba(14, 12, 8, 0.9);
    --grain-opacity: 0.035;
}

/* ═══════════════════════════════════════════════════════════
   RESET & BASE
   ═══════════════════════════════════════════════════════════ */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

html {
    scroll-behavior: smooth;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
}

body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    font-weight: 300;
    background-color: var(--bg-primary);
    color: var(--text-primary);
    line-height: 1.7;
    transition: background-color 0.7s cubic-bezier(0.4, 0, 0.2, 1),
                color 0.7s cubic-bezier(0.4, 0, 0.2, 1);
    overflow-x: hidden;
}

::selection {
    background: var(--accent);
    color: var(--bg-primary);
}

img { max-width: 100%; height: auto; display: block; }
a { color: inherit; text-decoration: none; }

/* ═══════════════════════════════════════════════════════════
   GRAIN TEXTURE OVERLAY
   ═══════════════════════════════════════════════════════════ */
.grain-overlay {
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 9999;
    opacity: var(--grain-opacity);
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    background-size: 256px 256px;
}

/* ═══════════════════════════════════════════════════════════
   LOADING SCREEN
   ═══════════════════════════════════════════════════════════ */
.loader {
    position: fixed;
    inset: 0;
    background: var(--bg-primary);
    z-index: 10000;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    transition: opacity 0.8s ease, visibility 0.8s ease;
}
.loader.hidden { opacity: 0; visibility: hidden; pointer-events: none; }
.loader-line {
    width: 60px;
    height: 1px;
    background: var(--accent);
    animation: loaderPulse 1.2s ease-in-out infinite;
}
@keyframes loaderPulse {
    0%, 100% { opacity: 0.2; transform: scaleX(0.3); }
    50% { opacity: 1; transform: scaleX(1); }
}

/* ═══════════════════════════════════════════════════════════
   NAVIGATION
   ═══════════════════════════════════════════════════════════ */
.nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    padding: 2.5rem 4rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}
.nav.scrolled {
    padding: 1.2rem 4rem;
    background: var(--bg-primary);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--border);
}
.nav-logo {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: 1.4rem;
    font-weight: 400;
    letter-spacing: 0.08em;
    color: var(--text-primary);
    transition: color 0.3s ease;
}
.nav-logo:hover { color: var(--accent); }
.nav-links {
    display: flex;
    gap: 3rem;
    list-style: none;
}
.nav-links a {
    font-size: 0.78rem;
    font-weight: 400;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--text-secondary);
    position: relative;
    padding-bottom: 4px;
    transition: color 0.3s ease;
}
.nav-links a::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 1px;
    background: var(--accent);
    transition: width 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}
.nav-links a:hover,
.nav-links a.active { color: var(--accent); }
.nav-links a:hover::after,
.nav-links a.active::after { width: 100%; }

/* Hamburger */
.nav-toggle {
    display: none;
    flex-direction: column;
    gap: 6px;
    cursor: pointer;
    background: none;
    border: none;
    padding: 8px;
    z-index: 1001;
}
.nav-toggle span {
    width: 24px;
    height: 1.5px;
    background: var(--text-primary);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    transform-origin: center;
}
.nav-toggle.open span:nth-child(1) { transform: rotate(45deg) translate(5px, 5px); }
.nav-toggle.open span:nth-child(2) { opacity: 0; }
.nav-toggle.open span:nth-child(3) { transform: rotate(-45deg) translate(5px, -5px); }

/* ═══════════════════════════════════════════════════════════
   HERO SECTION
   ═══════════════════════════════════════════════════════════ */
.hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 2rem;
    position: relative;
    overflow: hidden;
}
.hero-bg {
    position: absolute;
    inset: -20%;
    background: radial-gradient(ellipse 60% 50% at 50% 50%, var(--accent-subtle), transparent);
    animation: heroBgPulse 10s ease-in-out infinite;
    will-change: transform;
}
@keyframes heroBgPulse {
    0%, 100% { opacity: 0.4; transform: scale(1) rotate(0deg); }
    33% { opacity: 0.7; transform: scale(1.05) rotate(1deg); }
    66% { opacity: 0.5; transform: scale(0.98) rotate(-1deg); }
}
.hero-content { position: relative; z-index: 1; }
.hero-name {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: clamp(3.5rem, 10vw, 9rem);
    font-weight: 300;
    line-height: 1;
    letter-spacing: -0.02em;
    margin-bottom: 1.5rem;
    overflow: hidden;
}
.hero-name .char {
    display: inline-block;
    opacity: 0;
    transform: translateY(100%) rotateX(90deg);
    animation: charReveal 0.8s cubic-bezier(0.23, 1, 0.32, 1) forwards;
}
@keyframes charReveal {
    to { opacity: 1; transform: translateY(0) rotateX(0deg); }
}
.hero-role {
    font-size: clamp(0.85rem, 1.5vw, 1.1rem);
    font-weight: 400;
    letter-spacing: 0.4em;
    text-transform: uppercase;
    color: var(--text-secondary);
    opacity: 0;
    animation: fadeUp 1s ease 0.8s forwards;
}
.hero-quote {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-style: italic;
    font-weight: 300;
    font-size: clamp(1.1rem, 2vw, 1.6rem);
    color: var(--text-muted);
    margin-top: 3rem;
    max-width: 600px;
    opacity: 0;
    animation: fadeUp 1s ease 1.2s forwards;
}
@keyframes fadeUp {
    from { opacity: 0; transform: translateY(25px); }
    to { opacity: 1; transform: translateY(0); }
}
.scroll-hint {
    position: absolute;
    bottom: 3rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    opacity: 0;
    animation: fadeUp 1s ease 1.8s forwards;
}
.scroll-hint span {
    font-size: 0.65rem;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--text-muted);
}
.scroll-line {
    width: 1px;
    height: 50px;
    background: linear-gradient(to bottom, var(--accent), transparent);
    animation: scrollPulse 2s ease-in-out infinite;
}
@keyframes scrollPulse {
    0%, 100% { opacity: 0; transform: scaleY(0); transform-origin: top; }
    50% { opacity: 0.6; transform: scaleY(1); transform-origin: top; }
}

/* ═══════════════════════════════════════════════════════════
   SECTIONS COMMON
   ═══════════════════════════════════════════════════════════ */
.section {
    padding: 10rem 4rem;
    max-width: 1400px;
    margin: 0 auto;
}
.section-label {
    font-size: 0.7rem;
    font-weight: 400;
    letter-spacing: 0.35em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 1.5rem;
    display: flex;
    align-items: center;
    gap: 1rem;
}
.section-label::before {
    content: '';
    width: 40px;
    height: 1px;
    background: var(--accent);
}
.section-title {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: clamp(2.2rem, 4.5vw, 4rem);
    font-weight: 300;
    line-height: 1.15;
    margin-bottom: 2.5rem;
    color: var(--text-primary);
}
.section-text {
    font-size: 1.05rem;
    line-height: 1.9;
    color: var(--text-secondary);
    max-width: 680px;
}

/* Reveal animations */
.reveal {
    opacity: 0;
    transform: translateY(40px);
    transition: opacity 0.9s cubic-bezier(0.23, 1, 0.32, 1),
                transform 0.9s cubic-bezier(0.23, 1, 0.32, 1);
}
.reveal.visible {
    opacity: 1;
    transform: translateY(0);
}
.reveal-delay-1 { transition-delay: 0.1s; }
.reveal-delay-2 { transition-delay: 0.2s; }
.reveal-delay-3 { transition-delay: 0.3s; }
.reveal-delay-4 { transition-delay: 0.4s; }

/* ═══════════════════════════════════════════════════════════
   ABOUT SECTION
   ═══════════════════════════════════════════════════════════ */
.about { background: var(--bg-primary); }
.about-inner {
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: 8rem;
    align-items: center;
}
.about-image-wrap {
    position: relative;
    overflow: hidden;
}
.about-image-wrap::before {
    content: '';
    position: absolute;
    top: -15px;
    left: -15px;
    right: 15px;
    bottom: 15px;
    border: 1px solid var(--accent);
    opacity: 0.2;
    z-index: 1;
    transition: opacity 0.6s ease;
}
.about-image-wrap:hover::before { opacity: 0.4; }
.about-image-wrap img {
    width: 100%;
    height: auto;
    filter: saturate(0.85) contrast(1.02);
    transition: filter 0.8s ease, transform 0.8s ease;
}
.about-image-wrap:hover img {
    filter: saturate(1) contrast(1);
    transform: scale(1.02);
}
.about-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin-top: 4rem;
    padding-top: 3rem;
    border-top: 1px solid var(--border);
}
.stat-number {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: 2.8rem;
    font-weight: 300;
    color: var(--accent);
    line-height: 1;
    margin-bottom: 0.5rem;
}
.stat-label {
    font-size: 0.75rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--text-muted);
}

/* ═══════════════════════════════════════════════════════════
   GALLERY SECTION
   ═══════════════════════════════════════════════════════════ */
.gallery {
    background: var(--bg-secondary);
    padding: 10rem 0;
    transition: background-color 0.7s ease;
}
.gallery-header {
    text-align: center;
    max-width: 700px;
    margin: 0 auto 6rem;
    padding: 0 4rem;
}
.gallery-header .section-label { justify-content: center; }
.gallery-header .section-label::before { display: none; }
.gallery-header .section-text { margin: 0 auto; text-align: center; }
.gallery-grid {
    columns: 3;
    column-gap: 1.5rem;
    padding: 0 3rem;
    max-width: 1600px;
    margin: 0 auto;
}
.gallery-item {
    break-inside: avoid;
    margin-bottom: 1.5rem;
    position: relative;
    overflow: hidden;
    border-radius: 3px;
    cursor: pointer;
    opacity: 0;
    transform: translateY(50px);
    transition: opacity 0.8s cubic-bezier(0.23, 1, 0.32, 1),
                transform 0.8s cubic-bezier(0.23, 1, 0.32, 1);
}
.gallery-item.visible {
    opacity: 1;
    transform: translateY(0);
}
.gallery-item img {
    width: 100%;
    display: block;
    transition: transform 0.8s cubic-bezier(0.23, 1, 0.32, 1),
                filter 0.8s ease;
    filter: saturate(0.9);
}
.gallery-item:hover img {
    transform: scale(1.04);
    filter: saturate(1.1) brightness(0.85);
}
.gallery-item-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(0,0,0,0.85) 0%, rgba(0,0,0,0.2) 40%, transparent 100%);
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: 2rem 1.5rem;
    opacity: 0;
    transition: opacity 0.5s ease;
}
.gallery-item:hover .gallery-item-overlay { opacity: 1; }
.gallery-item-overlay h3 {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: 1.35rem;
    font-weight: 400;
    color: #fff;
    margin-bottom: 0.3rem;
    transform: translateY(15px);
    transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1);
}
.gallery-item:hover .gallery-item-overlay h3 { transform: translateY(0); }
.gallery-item-overlay p {
    font-size: 0.78rem;
    color: rgba(255,255,255,0.65);
    letter-spacing: 0.05em;
    transform: translateY(15px);
    transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1) 0.05s;
}
.gallery-item:hover .gallery-item-overlay p { transform: translateY(0); }
.gallery-item-overlay .painting-desc {
    font-size: 0.75rem;
    color: rgba(255,255,255,0.5);
    margin-top: 0.5rem;
    font-style: italic;
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: 0.9rem;
    transform: translateY(15px);
    transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1) 0.1s;
}
.gallery-item:hover .gallery-item-overlay .painting-desc { transform: translateY(0); }

/* ═══════════════════════════════════════════════════════════
   LIGHTBOX
   ═══════════════════════════════════════════════════════════ */
.lightbox {
    position: fixed;
    inset: 0;
    background: var(--overlay);
    z-index: 2000;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.5s ease, visibility 0.5s ease;
    backdrop-filter: blur(30px);
    -webkit-backdrop-filter: blur(30px);
}
.lightbox.active { opacity: 1; visibility: visible; }
.lightbox-img-wrap {
    position: relative;
    max-width: 90vw;
    max-height: 85vh;
    display: flex;
    align-items: center;
    justify-content: center;
}
.lightbox-img-wrap img {
    max-width: 90vw;
    max-height: 85vh;
    object-fit: contain;
    border-radius: 4px;
    transform: scale(0.9);
    opacity: 0;
    transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1),
                opacity 0.5s ease;
}
.lightbox.active .lightbox-img-wrap img {
    transform: scale(1);
    opacity: 1;
}
.lightbox-close {
    position: absolute;
    top: 2rem;
    right: 2rem;
    width: 44px;
    height: 44px;
    background: none;
    border: 1px solid var(--border);
    border-radius: 50%;
    color: var(--text-primary);
    font-size: 1.3rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    z-index: 10;
}
.lightbox-close:hover {
    background: var(--accent);
    border-color: var(--accent);
    color: var(--bg-primary);
    transform: rotate(90deg);
}
.lightbox-nav {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 50px;
    height: 50px;
    background: none;
    border: 1px solid var(--border);
    border-radius: 50%;
    color: var(--text-primary);
    font-size: 1.5rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    z-index: 10;
}
.lightbox-nav:hover {
    background: var(--accent);
    border-color: var(--accent);
    color: var(--bg-primary);
}
.lightbox-prev { left: 2rem; }
.lightbox-next { right: 2rem; }
.lightbox-info {
    position: absolute;
    bottom: 3rem;
    left: 50%;
    transform: translateX(-50%);
    text-align: center;
    color: var(--text-primary);
    max-width: 600px;
}
.lightbox-info h3 {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: 1.5rem;
    font-weight: 400;
    margin-bottom: 0.3rem;
}
.lightbox-info p {
    font-size: 0.85rem;
    color: var(--text-secondary);
}
.lightbox-info .lb-desc {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-style: italic;
    font-size: 1rem;
    color: var(--text-muted);
    margin-top: 0.5rem;
}

/* ═══════════════════════════════════════════════════════════
   CONTACT SECTION
   ═══════════════════════════════════════════════════════════ */
.contact {
    text-align: center;
    background: var(--bg-primary);
}
.contact-inner {
    max-width: 600px;
    margin: 0 auto;
}
.contact-email {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: clamp(1.3rem, 2.5vw, 2rem);
    color: var(--accent);
    position: relative;
    display: inline-block;
    margin-top: 2.5rem;
    transition: color 0.3s ease;
}
.contact-email::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 100%;
    height: 1px;
    background: var(--accent);
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1);
}
.contact-email:hover { color: var(--accent-hover); }
.contact-email:hover::after { transform: scaleX(1); transform-origin: left; }

/* ═══════════════════════════════════════════════════════════
   FOOTER
   ═══════════════════════════════════════════════════════════ */
footer {
    padding: 4rem;
    text-align: center;
    border-top: 1px solid var(--border);
    background: var(--bg-secondary);
    transition: background-color 0.7s ease;
}
footer p {
    font-size: 0.78rem;
    color: var(--text-muted);
    letter-spacing: 0.12em;
}

/* ═══════════════════════════════════════════════════════════
   THEME SWITCHER
   ═══════════════════════════════════════════════════════════ */
.theme-switcher {
    position: fixed;
    right: 2rem;
    top: 50%;
    transform: translateY(-50%);
    z-index: 1001;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    padding: 1rem 0.7rem;
    background: var(--bg-card);
    border-radius: 2.5rem;
    border: 1px solid var(--border);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    box-shadow: 0 8px 32px var(--shadow);
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}
.theme-btn {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    border: 2px solid var(--border);
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.23, 1, 0.32, 1);
    position: relative;
    flex-shrink: 0;
}
.theme-btn:hover {
    transform: scale(1.25);
    border-color: var(--accent);
}
.theme-btn.active {
    transform: scale(1.15);
    border-color: var(--accent);
    box-shadow: 0 0 12px var(--accent-glow);
}
.theme-btn[data-theme="ejszaka"] {
    background: conic-gradient(#d4a853 0deg, #080808 90deg, #d4a853 180deg, #080808 270deg);
}
.theme-btn[data-theme="hajnal"] {
    background: conic-gradient(#c4884a 0deg, #f7f2ea 90deg, #c4884a 180deg, #f7f2ea 270deg);
}
.theme-btn[data-theme="oazis"] {
    background: conic-gradient(#d4a853 0deg, #0e1a0e 90deg, #d4a853 180deg, #0e1a0e 270deg);
}
.theme-btn[data-theme="vaszon"] {
    background: conic-gradient(#5a7a4a 0deg, #f5f2ec 90deg, #5a7a4a 180deg, #f5f2ec 270deg);
}
.theme-btn[data-theme="tuz-es-jeg"] {
    background: conic-gradient(#e04060 0deg, #0a0c14 90deg, #e04060 180deg, #0a0c14 270deg);
}
.theme-btn[data-theme="pasztell"] {
    background: conic-gradient(#c47a6a 0deg, #faf5f2 90deg, #c47a6a 180deg, #faf5f2 270deg);
}
.theme-btn[data-theme="erdo"] {
    background: conic-gradient(#c49040 0deg, #0e0c08 90deg, #c49040 180deg, #0e0c08 270deg);
}
.theme-tooltip {
    position: absolute;
    right: calc(100% + 1rem);
    top: 50%;
    transform: translateY(-50%);
    background: var(--bg-card);
    color: var(--text-primary);
    padding: 0.4rem 0.8rem;
    border-radius: 0.4rem;
    font-size: 0.68rem;
    letter-spacing: 0.08em;
    white-space: nowrap;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition: all 0.3s ease;
    border: 1px solid var(--border);
    box-shadow: 0 4px 12px var(--shadow);
}
.theme-btn:hover .theme-tooltip {
    opacity: 1;
    visibility: visible;
    right: calc(100% + 0.8rem);
}

/* ═══════════════════════════════════════════════════════════
   RESPONSIVE
   ═══════════════════════════════════════════════════════════ */
@media (max-width: 1200px) {
    .gallery-grid { columns: 2; padding: 0 2rem; }
    .about-inner { gap: 5rem; }
}

@media (max-width: 1024px) {
    .nav { padding: 1.5rem 2.5rem; }
    .nav.scrolled { padding: 1rem 2.5rem; }
    .section { padding: 7rem 2.5rem; }
    .about-inner {
        grid-template-columns: 1fr;
        gap: 4rem;
    }
    .about-image-wrap { max-width: 500px; margin: 0 auto; }
    .gallery-grid { columns: 2; padding: 0 2rem; }
    .lightbox-nav { display: none; }
}

@media (max-width: 768px) {
    .nav { padding: 1.2rem 1.5rem; }
    .nav.scrolled { padding: 0.8rem 1.5rem; }
    .nav-links {
        position: fixed;
        inset: 0;
        background: var(--bg-primary);
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 2.5rem;
        opacity: 0;
        visibility: hidden;
        transition: all 0.5s ease;
    }
    .nav-links.open { opacity: 1; visibility: visible; }
    .nav-links a { font-size: 1rem; letter-spacing: 0.25em; }
    .nav-toggle { display: flex; }
    .section { padding: 6rem 1.5rem; }
    .gallery { padding: 6rem 0; }
    .gallery-grid {
        columns: 1;
        padding: 0 1.5rem;
    }
    .about-stats { grid-template-columns: repeat(3, 1fr); gap: 1rem; }
    .stat-number { font-size: 2rem; }
    .theme-switcher {
        right: 0.5rem;
        top: auto;
        bottom: 1rem;
        transform: none;
        flex-direction: row;
        border-radius: 1.5rem;
        padding: 0.6rem 0.5rem;
        gap: 0.4rem;
    }
    .theme-btn { width: 24px; height: 24px; }
    .theme-tooltip { display: none; }
    .lightbox-nav { display: none; }
    .hero-quote { font-size: 1rem; padding: 0 1rem; }
}

@media (max-width: 480px) {
    .hero-name { font-size: clamp(2.5rem, 12vw, 4rem); }
    .about-stats { grid-template-columns: 1fr; text-align: center; }
}

/* ═══════════════════════════════════════════════════════════
   DECORATIVE ELEMENTS
   ═══════════════════════════════════════════════════════════ */
.section-divider {
    width: 60px;
    height: 1px;
    background: var(--accent);
    margin: 0 auto;
    opacity: 0.4;
}
.ornament {
    text-align: center;
    padding: 2rem 0;
    color: var(--text-muted);
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-size: 1.5rem;
    letter-spacing: 1em;
    opacity: 0.4;
}
</style>
</head>
<body>

<!-- Grain overlay -->
<div class="grain-overlay"></div>

<!-- Loading screen -->
<div class="loader" id="loader">
    <div class="loader-line"></div>
</div>

<!-- Navigation -->
<nav class="nav" id="navbar">
    <a href="#hero" class="nav-logo">Kárai Mihály</a>
    <ul class="nav-links" id="navLinks">
        <li><a href="#about" data-nav>Rólam</a></li>
        <li><a href="#gallery" data-nav>Galéria</a></li>
        <li><a href="#contact" data-nav>Kapcsolat</a></li>
    </ul>
    <button class="nav-toggle" id="navToggle" aria-label="Menü">
        <span></span><span></span><span></span>
    </button>
</nav>

<!-- Hero -->
<section class="hero" id="hero">
    <div class="hero-bg"></div>
    <div class="hero-content">
        <h1 class="hero-name" id="heroName">Kárai Mihály</h1>
        <p class="hero-role">Festőművész</p>
        <p class="hero-quote">„A festészet az érzelmek nyelve, amely szavak nélkül beszél"</p>
    </div>
    <div class="scroll-hint">
        <span>Felfedezés</span>
        <div class="scroll-line"></div>
    </div>
</section>

<!-- About -->
<section class="section about" id="about">
    <div class="about-inner">
        <div class="about-image-wrap reveal">
            <img src="../apa_festmenyek/interior_1.jpg" alt="Kárai Mihály alkotótere">
        </div>
        <div>
            <div class="reveal">
                <span class="section-label">A művész</span>
                <h2 class="section-title">Érzelmek színekben,<br>gondolatok ecsetvonásokban</h2>
            </div>
            <div class="reveal reveal-delay-1">
                <p class="section-text">
                    Kárai Mihály (1961) kortárs festőművész, aki munkásságában az absztrakt expresszionizmus és a lírai absztrakció határán mozog. Akril- és olajfestékekkel, vastag impasto technikával, palettakéses kaparásokkal dolgozik, létrehozva olyan műveket, amelyek az érzelmek mélységét és a természet erőit idézik.
                </p>
            </div>
            <div class="reveal reveal-delay-2">
                <p class="section-text">
                    Műveit dominálja a meleg spektrum – tűzpiros, kadmiumnarancs, napraforgósárga, arany – melyeket erős kontrasztot képező hideg színek (ciánkék, sötétkék, türkiz, smaragdzöld) metszenek át. Kompozíciói térbeli mélységet, elmosódó horizontokat, ritmikus geometriai blokkokat és organikus formákat ötvöznek.
                </p>
            </div>
            <div class="about-stats reveal reveal-delay-3">
                <div class="stat-item">
                    <div class="stat-number" data-count="1961">1961</div>
                    <div class="stat-label">Születés éve</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number" data-count="40">40+</div>
                    <div class="stat-label">Év tapasztalat</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number" data-count="100">100+</div>
                    <div class="stat-label">Alkotás</div>
                </div>
            </div>
        </div>
    </div>
</section>

<div class="ornament">· · ·</div>

<!-- Gallery -->
<section class="gallery" id="gallery">
    <div class="gallery-header reveal">
        <span class="section-label">Galéria</span>
        <h2 class="section-title">Válogatás az alkotásokból</h2>
        <p class="section-text">Minden festmény egy külön világ, egy érzelmi utazás, egy pillanat, amely örökké megmarad a vásznon.</p>
    </div>
    <div class="gallery-grid" id="galleryGrid"></div>
</section>

<!-- Contact -->
<section class="section contact" id="contact">
    <div class="contact-inner">
        <div class="reveal">
            <span class="section-label" style="justify-content:center;">
                <span style="display:none;"></span>Kapcsolat
            </span>
            <h2 class="section-title">Kérjen személyes ajánlást</h2>
            <p class="section-text" style="margin: 0 auto;">Ha érdekel egy alkotás, vagy egyedi megrendelésre festek, vegye fel velem a kapcsolatot.</p>
            <a href="mailto:karai.mihaly@email.hu" class="contact-email">karai.mihaly@email.hu</a>
        </div>
    </div>
</section>

<!-- Footer -->
<footer>
    <p>© 2026 Kárai Mihály – Minden jog fenntartva</p>
</footer>

<!-- Theme Switcher -->
<div class="theme-switcher" id="themeSwitcher">
    <button class="theme-btn active" data-theme="hajnal" aria-label="Hajnal téma">
        <span class="theme-tooltip">Hajnal</span>
    </button>
    <button class="theme-btn" data-theme="vaszon" aria-label="Vászon téma">
        <span class="theme-tooltip">Vászon</span>
    </button>
    <button class="theme-btn" data-theme="pasztell" aria-label="Pasztell téma">
        <span class="theme-tooltip">Pasztell</span>
    </button>
    <button class="theme-btn" data-theme="ejszaka" aria-label="Éjszaka téma">
        <span class="theme-tooltip">Éjszaka</span>
    </button>
    <button class="theme-btn" data-theme="oazis" aria-label="Oázis téma">
        <span class="theme-tooltip">Oázis</span>
    </button>
    <button class="theme-btn" data-theme="tuz-es-jeg" aria-label="Tűz és Jég téma">
        <span class="theme-tooltip">Tűz és Jég</span>
    </button>
    <button class="theme-btn" data-theme="erdo" aria-label="Erdő téma">
        <span class="theme-tooltip">Erdő</span>
    </button>
</div>

<!-- Lightbox -->
<div class="lightbox" id="lightbox">
    <button class="lightbox-close" id="lightboxClose" aria-label="Bezárás">×</button>
    <button class="lightbox-nav lightbox-prev" id="lightboxPrev" aria-label="Előző">‹</button>
    <button class="lightbox-nav lightbox-next" id="lightboxNext" aria-label="Következő">›</button>
    <div class="lightbox-img-wrap">
        <img src="" alt="" id="lightboxImg">
    </div>
    <div class="lightbox-info">
        <h3 id="lightboxTitle"></h3>
        <p id="lightboxMeta"></p>
        <p class="lb-desc" id="lightboxDesc"></p>
    </div>
</div>

<script>
/* ═══════════════════════════════════════════════════════════
   PAINTING DATA
   ═══════════════════════════════════════════════════════════ */
const paintings = [
    {
        title: "Absztrakt festmény",
        size: "80 × 60 cm",
        medium: "Akril/Olaj vásznon",
        file: "../apa_festmenyek/014fb97bae01374ff673a7f51de018af-karai-mihaly-absztrakt-festmeny-kepmeret-80x60-cm.jpg",
        desc: "Meleg tónusú, textúrált kompozíció rácsszerű szerkezettel"
    },
    {
        title: "A kép mérete",
        size: "80 × 60 cm",
        medium: "Akril/Olaj vásznon",
        file: "../apa_festmenyek/579ee3610e2f00108ae1ad3b4efe881f-karai-mihaly-a-kep-merete-80x60-cm.jpg",
        desc: "Geometriai color-blocking, erős vizuális dualitás"
    },
    {
        title: "Színes modern absztrakt",
        size: "90 × 90 cm",
        medium: "Akril/Olaj vásznon",
        file: "../apa_festmenyek/732e9d819fbbee1e6b2126f580f51743-karai-mihaly-szines-modern-absztrakt-festmeny-90-x-90-cm.jpg",
        desc: "Szürreális, organikus formák – tűz és jég összecsapása"
    },
    {
        title: "Panoráma város",
        size: "140 × 40 cm",
        medium: "Akril/Olaj vásznon",
        file: "../apa_festmenyek/76a52a2610ed517750b4d73efabbe595-karai-mihaly-absztrakt-festmeny-140x40x4-cm.jpg",
        desc: "Panorámaszerű függőleges elrendezés, éjszakai város"
    },
    {
        title: "Izzó torony",
        size: "140 × 40 cm",
        medium: "Akril/Olaj vásznon",
        file: "../apa_festmenyek/8548dfcda68a1463e1ed276bce0b0b0c-karai-mihaly-absztrakt-festmeny-kepmeret-140x40-cm.jpg",
        desc: "Vérvörös égbolt, izzó torony, hullámzó tükör"
    },
    {
        title: "Sivatagi oázis",
        size: "40 × 70 cm",
        medium: "Akril/Olaj vásznon",
        file: "../apa_festmenyek/9714f6cd20df8b60a6cfa68e340bdcb2-karai-mihaly-absztrakt-festmeny-40x70-cm.jpg",
        desc: "Horizontális tájkép-absztrakció, meditatív kompozíció"
    },
    {
        title: "Kortárs absztrakt",
        size: "70 × 30 cm",
        medium: "Akril/Olaj vásznon",
        file: "../apa_festmenyek/c74fb23ff889ff7fc5bc22904e957ac7-karai-mihaly-kortars-absztrakt-festmeny-kepmeret-70x30-cm.jpg",
        desc: "Két világ találkozása, fénysugár és víztükör"
    },
    {
        title: "Nyári hőség",
        size: "100 × 70 cm",
        medium: "Akril/Olaj vásznon",
        file: "../apa_festmenyek/cf6c30f8ab61eea749199ee138ae02df-karai-mihaly-nyari-hoseg-kepmeret-100x70-cm.jpg",
        desc: "Figurális-absztrakt fúzió, a nyári nap fizikai ereje"
    },
    {
        title: "Szív motívum",
        size: "70 × 70 cm",
        medium: "Akril/Olaj vásznon",
        file: "../apa_festmenyek/d0e8f21d863d3647ce7ce2705fbb0aeb-karai-mihaly-absztrakt-festmeny-kepmeret-70x70-cm.jpg",
        desc: "Nyers, indusztriális textúra, a szenvedély pirosa"
    },
    {
        title: "Pasztell atmoszféra",
        size: "90 × 100 cm",
        medium: "Akril/Olaj vásznon",
        file: "../apa_festmenyek/d45190df18d013a48540242d4e02b588-karai-mihaly-szines-modern-absztrakt-festmeny-90x100-cm.jpg",
        desc: "Finom, elegáns, pasztell atmoszféra – virágszirmok"
    },
    {
        title: "Városi mozaik",
        size: "60 × 60 cm",
        medium: "Akril/Olaj vásznon",
        file: "../apa_festmenyek/e862397c8ee36bfab0bbe2de56b503e2-karai-mihaly-absztrakt-festmeny-kepmeret-60x60-cm.jpg",
        desc: "Lüktető városi mozaik, madártávlati perspektíva"
    },
    {
        title: "Destrukció",
        size: "70 × 100 cm",
        medium: "Akril/Olaj vásznon",
        file: "../apa_festmenyek/keretnelkul_1.jpg",
        desc: "A legsötétebb, legagresszívebb tónusú darab – chiaroscuro"
    },
    {
        title: "Hangulat",
        size: "80 × 60 cm",
        medium: "Akril/Olaj vásznon",
        file: "../apa_festmenyek/keretnelkul_2.jpg",
        desc: "Áramló, lüktető tiszta energia – misztikus és érzelmes"
    }
];

/* ═══════════════════════════════════════════════════════════
   HERO LETTER ANIMATION
   ═══════════════════════════════════════════════════════════ */
(function initHeroName() {
    const heroName = document.getElementById('heroName');
    const text = heroName.textContent;
    heroName.innerHTML = '';
    [...text].forEach((char, i) => {
        const span = document.createElement('span');
        span.className = 'char';
        span.textContent = char === ' ' ? '\u00A0' : char;
        span.style.animationDelay = `${0.3 + i * 0.04}s`;
        heroName.appendChild(span);
    });
})();

/* ═══════════════════════════════════════════════════════════
   LOADER
   ═══════════════════════════════════════════════════════════ */
window.addEventListener('load', () => {
    setTimeout(() => {
        document.getElementById('loader').classList.add('hidden');
    }, 600);
});

/* ═══════════════════════════════════════════════════════════
   NAVIGATION
   ═══════════════════════════════════════════════════════════ */
const navbar = document.getElementById('navbar');
const navToggle = document.getElementById('navToggle');
const navLinks = document.getElementById('navLinks');

window.addEventListener('scroll', () => {
    navbar.classList.toggle('scrolled', window.scrollY > 80);
});

navToggle.addEventListener('click', () => {
    navToggle.classList.toggle('open');
    navLinks.classList.toggle('open');
});

// Close mobile menu on link click
navLinks.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', () => {
        navToggle.classList.remove('open');
        navLinks.classList.remove('open');
    });
});

// Active nav link on scroll
const sections = document.querySelectorAll('section[id]');
const navAnchors = document.querySelectorAll('[data-nav]');

window.addEventListener('scroll', () => {
    let current = '';
    sections.forEach(section => {
        const sectionTop = section.offsetTop - 200;
        if (window.scrollY >= sectionTop) {
            current = section.getAttribute('id');
        }
    });
    navAnchors.forEach(a => {
        a.classList.toggle('active', a.getAttribute('href') === `#${current}`);
    });
});

/* ═══════════════════════════════════════════════════════════
   THEME SWITCHER
   ═══════════════════════════════════════════════════════════ */
const themeButtons = document.querySelectorAll('.theme-btn');
const savedTheme = localStorage.getItem('karai-theme') || 'hajnal';
document.documentElement.setAttribute('data-theme', savedTheme);
themeButtons.forEach(btn => {
    btn.classList.toggle('active', btn.dataset.theme === savedTheme);
});

themeButtons.forEach(btn => {
    btn.addEventListener('click', () => {
        const theme = btn.dataset.theme;
        document.documentElement.setAttribute('data-theme', theme);
        localStorage.setItem('karai-theme', theme);
        themeButtons.forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
    });
});

/* ═══════════════════════════════════════════════════════════
   SCROLL REVEAL
   ═══════════════════════════════════════════════════════════ */
const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add('visible');
        }
    });
}, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });

document.querySelectorAll('.reveal').forEach(el => revealObserver.observe(el));

/* ═══════════════════════════════════════════════════════════
   GALLERY
   ═══════════════════════════════════════════════════════════ */
const galleryGrid = document.getElementById('galleryGrid');

paintings.forEach((painting, index) => {
    const item = document.createElement('div');
    item.className = 'gallery-item';
    item.style.transitionDelay = `${index * 0.07}s`;
    item.innerHTML = `
        <img src="${painting.file}" alt="${painting.title}" loading="lazy">
        <div class="gallery-item-overlay">
            <h3>${painting.title}</h3>
            <p>${painting.size} · ${painting.medium}</p>
            <span class="painting-desc">${painting.desc}</span>
        </div>
    `;
    item.addEventListener('click', () => openLightbox(index));
    galleryGrid.appendChild(item);
});

const galleryItems = document.querySelectorAll('.gallery-item');
const galleryObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add('visible');
        }
    });
}, { threshold: 0.05, rootMargin: '0px 0px -20px 0px' });

galleryItems.forEach(item => galleryObserver.observe(item));

/* ═══════════════════════════════════════════════════════════
   LIGHTBOX
   ═══════════════════════════════════════════════════════════ */
const lightbox = document.getElementById('lightbox');
const lightboxImg = document.getElementById('lightboxImg');
const lightboxTitle = document.getElementById('lightboxTitle');
const lightboxMeta = document.getElementById('lightboxMeta');
const lightboxDesc = document.getElementById('lightboxDesc');
let currentIndex = 0;

function openLightbox(index) {
    currentIndex = index;
    updateLightbox();
    lightbox.classList.add('active');
    document.body.style.overflow = 'hidden';
}

function updateLightbox() {
    const p = paintings[currentIndex];
    lightboxImg.src = p.file;
    lightboxImg.alt = p.title;
    lightboxTitle.textContent = p.title;
    lightboxMeta.textContent = `${p.size} · ${p.medium}`;
    lightboxDesc.textContent = p.desc;
}

function closeLightbox() {
    lightbox.classList.remove('active');
    document.body.style.overflow = '';
}

document.getElementById('lightboxClose').addEventListener('click', closeLightbox);
lightbox.addEventListener('click', (e) => {
    if (e.target === lightbox) closeLightbox();
});

document.getElementById('lightboxPrev').addEventListener('click', (e) => {
    e.stopPropagation();
    currentIndex = (currentIndex - 1 + paintings.length) % paintings.length;
    updateLightbox();
});

document.getElementById('lightboxNext').addEventListener('click', (e) => {
    e.stopPropagation();
    currentIndex = (currentIndex + 1) % paintings.length;
    updateLightbox();
});

document.addEventListener('keydown', (e) => {
    if (!lightbox.classList.contains('active')) return;
    if (e.key === 'Escape') closeLightbox();
    if (e.key === 'ArrowLeft') {
        currentIndex = (currentIndex - 1 + paintings.length) % paintings.length;
        updateLightbox();
    }
    if (e.key === 'ArrowRight') {
        currentIndex = (currentIndex + 1) % paintings.length;
        updateLightbox();
    }
});

/* ═══════════════════════════════════════════════════════════
   SMOOTH SCROLL
   ═══════════════════════════════════════════════════════════ */
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function(e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
            target.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
    });
});
</script>
</body>
</html>
